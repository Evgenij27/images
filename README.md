Before start set properties in application properties:

```properties
apikey={apiKey}
fetch.rate={fetch rate in milliseconds}
fetch.delay={delay before fetch in milliseconds}
```

To start: 
```bash
mvn spring-boot:run
```

